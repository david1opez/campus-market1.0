export type BusinessPostProps = {
    title: string,
    description: string,
    image: string,
    openingHours: [string, string],
    verified: boolean,
    id: string,
    opacity?: any,
}